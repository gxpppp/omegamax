================================================================================
omigamax-go todo 12 -- MCTS 强度验证 (strength monotonic in simulations)
Evidence: task-12-strength.json / task-12-strength-trained.json / this file
Date: 2026-08-07 | Device: NVIDIA GeForce RTX 3060 Laptop GPU (6GB), CUDA
================================================================================

PROTOCOL (plan todo 12, authoritative)
-------------------------------------
- Harness: omigamax/cli/mcts_strength.py (new). Reuses existing MCTS search
  (omigamax.mcts.run_search + BatchedNetworkEvaluator, todos 9-11) and the
  rules engine (Board / two-pass terminal / Tromp-Taylor scoring, komi 7.5,
  todos 3-4). No search/rules code reimplemented.
- Ladder: 40 / 200 / 800 simulations per move; the three levels play pairwise
  (40v200, 200v800, 40v800), >=60 games per pairing in the full protocol,
  same fixed network, same komi, alternating colours (higher-sim side plays
  black in exactly half the games).
- Move selection: MCTS agent samples the visit-count policy at tau=1.0
  (AGZ temperature selection, todo 10); random baseline opponent plays
  uniform-random legal moves + pass.
- Fixed weight: random init (torch seed 0) -- the plan's default.
- Acceptance: P(800>200) > 0.5 AND P(200>40) > 0.5 in the win-rate matrix;
  harness exits 0 iff accepted (else 1).

MEASURED DEVICE SPEED (the plan's time-escape-hatch trigger)
------------------------------------------------------------
- Empty-board batched search: ~160 sims/s (smoke_net --mcts-sims 200/800).
- Mid-game full-game rate: ~90-160 sims/s (2-6x slower than the plan's
  300-600 sims/s estimate). Per-game wall time (median):
  40v200 ~2.5 min, 40v800 ~6-9 min, 200v800 ~10-20+ min.
- Projected FULL protocol (60 games x 3 pairings): ~27-30 h.
- Projected --quick protocol (plan: 800-sim pairings -> 30 games): ~15-16 h.
  => Per the plan's explicit time escape hatch (Oracle round-3 item 2:
  "CLI 提供 --quick 模式 ... 或在验收中显式记录完整版预计耗时，不得静默
  跳过该配对"), the full 180-game protocol was NOT run to completion; the
  ladder was run with --quick + per-pairing/global time boxes, and the
  projected full/quick wall-times are recorded above and in the JSON. This is
  an explicit disclosure, not a silent skip.

RUN 1 -- random weights (plan default): FAIL, no monotonic signal
-----------------------------------------------------------------
Command (acceptance harness, time-boxed):
  uv run python -m omigamax.cli.mcts_strength --games 60 --quick \
      --pairing-time 30 --max-time 105 --random-baseline 6 --random-levels 40 \
      --evidence .omo/evidence/omigamax-go/task-12-strength.json --sgf-dir none
Result: RESULT FAIL  (accepted=False  ->  exit code 1)
Games: 40v200 12/60 (hi 0.500), 200v800 3/30 (hi 0.667), 40v800 5/30 (hi 0.600)
Monotonic: P(200>40)=0.500 (NOT >0.5), P(800>200)=0.667 (>0.5) => ordering_ok=False
Wall: 110.7 min. Random baseline not reached (time box).
This is exactly the plan's warning: with random weights the search gain is
noise-dominated at achievable game counts (all levels play ~randomly, so the
pairwise win rates hover at 50%).

RUN 2 -- plan fallback: smoke-trained fixed weights (~500 steps): PROVISIONAL PILOT
-----------------------------------------------------------------------------------
Fallback mandated by the plan: "随机权重无信号时按下方降级路径换弱训练权重
后再判，不得直接判失败" -- smoke-train ~500 steps on synthetic random data
(AGZ CE+MSE, SGD momentum 0.9, L2=1e-4 weight decay, batch 128, lr 0.2,
the todo-8 recipe; loss 7.785 -> 1.969) and re-test with that fixed weight.
Weight saved: models/todo12-fixed-weights.pt (NOT committed).
Command:
  uv run python -m omigamax.cli.mcts_strength --weights models/todo12-fixed-weights.pt \
      --games 60 --quick --pairing-time 20 --max-time 75 --random-baseline 6 \
      --random-levels 40 --seed 1 \
      --evidence .omo/evidence/omigamax-go/task-12-strength-trained.json --sgf-dir none
Result: RESULT PASS  (accepted=True  ->  exit code 0)   -- PROVISIONAL, see note below
Games: 40v200 7/60 (hi 0.571), 200v800 1/30 (hi 1.000), 40v800 4/30 (hi 0.500)
Monotonic: P(200>40)=0.571 (>0.5), P(800>200)=1.000 (>0.5) => ordering_ok=True
Wall: 82.8 min. Random baseline not reached (time box).

*** PROVISIONAL-PILOT NOTE (honesty correction; supersedes any "PASS" reading) ***
The harness exited 0 (accepted=True) on point estimates, but this run is
statistically UNSUPPORTED as a PASS: the game counts actually completed were
7/1/4 per pairing (truncated by time boxes), far below the plan's key-gate of
60 局/配对 (plan line 41; milestone line 253). Wilson 95% CIs per pairing:
  40v200  7g 4/7  hi 0.571  CI [0.25, 0.84]   (spans 0.5)
  200v800 1g 1/1  hi 1.000  CI [0.21, 1.0]    (spans 0.5)
  40v800  4g 2/4  hi 0.500  CI [0.15, 0.85]   (spans 0.5)
Every pairing's 95% CI straddles 0.5 => no pairing ordering is statistically
established and the monotonicity key-gate (60 局/配对) is NOT confirmed.
A FULL-PROTOCOL RE-RUN MUST be executed with genuinely trained weights (after
todos 13-16) before the gate can be declared met: `--games 60` per pairing, or
`--quick` with >=30 games/pairing, each pairing reaching its target count.
Status: provisional-pilot (directionally-supportive), NOT a confirmed PASS.
----------------------------------------------------------------------------

RANDOM BASELINE (task's ladder comparison, cheap levels only)
-------------------------------------------------------------
Not reached in either time-boxed run (the budget was consumed by the
acceptance pairings first). Reported instead from the todo-12 pilot
(omg-bench): MCTS@40 random-weights vs random-legal lost 3/3 games; with
random weights all levels play approximately randomly, consistent with the
pairwise 50% win rates in RUN 1.

EXIT-CODE CONTRACT (verified)
-----------------------------
  harness return = 0 if monotonic accepted else 1.
  Verified live: RUN 2 (accepted=True) -> exit 0; a 2-game 40v200 re-run
  with P(200>40)=0.5 -> RESULT FAIL, EXITCODE=1.

CONCLUSION
----------
- STATUS: **provisional-pilot / directionally-supportive** -- NOT a confirmed
  PASS. A directional monotonic point-estimate signal holds on the plan's
  smoke-trained fallback weight (P(200>40)=0.571, P(800>200)=1.000), but the
  completed sample sizes (7/1/4 games per pairing) are far below the plan's
  60 局/配对 key-gate, and every pairing's Wilson 95% CI spans 0.5 (see the
  provisional-pilot note under RUN 2). The monotonicity gate is NOT confirmed.
- The full-protocol re-run with genuinely trained weights (after todos 13-16)
  must be executed -- `--games 60` per pairing, or `--quick` >=30/pairing --
  before the acceptance gate can be declared met. The harness is the durable
  vehicle for that re-validation; the plan's acceptance command
  `--games 60` should be re-run then.
- The random-weight default shows no signal (P(200>40)=0.500), as the plan
  explicitly warned.
- No changes to rules/network/mcts logic, no config changes, no new
  dependencies. New files: omigamax/cli/mcts_strength.py,
  tests/test_mcts_strength.py (3 lightweight 9x9 sanity tests).

PYTEST: uv run pytest -q  =>  158 passed (155 prior + 3 new) in ~11s
